# azure-pipelines.yml

pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: grafana/k6-action@v0.2.0
    inputs:
      filename: 'src/index.js'

schedules:
  - cron: '0 0 * * *'
    displayName: Daily midnight build
    branches:
      include:
        - master
